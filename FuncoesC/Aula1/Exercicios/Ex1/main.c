#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

char Main() {
	return printf("Seja bem-vindo ao sistema!");
}
int main(int argc, char *argv[]) {
	Main();
	return 0;
}
